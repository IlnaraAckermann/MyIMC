<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myimc">

    <!-- Permissões (se necessário) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Adicione outras permissões conforme necessário -->

    <!-- Defina a atividade principal -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyIMC">

        <!-- Atividade principal -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outras atividades e componentes podem ser definidos aqui, se necessário -->

    </application>

</manifest>
